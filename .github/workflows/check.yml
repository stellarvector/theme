name: Theme Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Gallery Check
        run: ./hugo/scripts/check-gallery.sh

      - name: Run Theme Lint
        run: ./hugo/scripts/lint-theme.sh
