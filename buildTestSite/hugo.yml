baseURL: "https://localhost:1313/"
title: "Stellar Vector Theme"

build:
  writeStats: true

markup:
  goldmark:
    renderer:
      unsafe: true

module:
  imports:
    - path: "github.com/stellarvector/theme"